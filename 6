string = "This is a sample string, with spaces. And, commas too."
replacements = [',', ' ', '.']
new_string = string

for r in replacements:
    new_string = new_string.replace(r, ':')

print(new_string)